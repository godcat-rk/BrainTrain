# BrainTrain 現状レポート（2026-07-13 調査）

調査セッションの成果物。次セッション以降の起点にする。
このセッションではコード変更なし（調査のためのビルド・テスト実行のみ）。

---

## 1. 現状マップ

### プロジェクト概要

- 脳トレ3ゲーム（神経衰弱・N-back・コルシブロック）を収録した React SPA
- 公開中: **https://godcat-rk.github.io/BrainTrain/** （2026-07-13 時点で HTTP 200、稼働確認済み）
- ソース本体は約 2,100 行とコンパクト。「氾濫」の主因はコードではなく **周辺（ブランチ・残骸ファイル・ドキュメント乖離）**

### ディレクトリの役割

| パス | 状態 | 備考 |
|------|------|------|
| `src/` | **本体・健全** | pages/games/（UI 3本）+ games/（ロジックフック3本）+ Dashboard/Home |
| `src/games/*/use*.ts` | 本体 | ゲームロジックのフック層。CLAUDE.md に未記載 |
| `android/` + `capacitor.config.ts` | **生きているが未完** | Capacitor 8 の Android プロジェクト（git 管理下）。`android/app/build/outputs/` には logs のみで **APK/AAB の成果物なし** → ビルド完走・実機確認に至ったか不明（要確認 #1） |
| `e2e/` | 本体 | corsi.spec.ts 1本のみ（9テスト）。memory / nback の E2E なし |
| `Dockerfile` + `nginx.conf` + `docker-compose.yml` | 用途重複気味 | ローカル動作確認用（CLAUDE.md 記載どおり）。GitHub Pages 運用が確立した今、役割が薄い（要確認 #4） |
| `dist/` | gitignore 済み | 問題なし |
| ルートの PNG 15枚 | **残骸** | untracked 5枚（corsi-*.png, home.png / 5月9日のデバッグ用らしき）+ gitignore 済み 10枚（screenshot-*.png / 5月4日） |
| `test-results/` | 残骸 | 空ディレクトリ（Playwright の出力先） |
| `.playwright-mcp/` | 残骸 | 5月の MCP ブラウザ操作ログ約40件。gitignore 済みだがローカルに堆積 |
| `src/App.css`, `src/assets/hero.png`, `react.svg`, `vite.svg` | **未使用（Vite テンプレ残骸）** | grep で参照ゼロを確認済み。git 管理下 |

### git の状態

- **活動履歴**: 全23コミットが 2026-05-03〜05-09 の1週間に集中。以降 **2ヶ月間休止**
- **ブランチ**:
  - `main`（HEAD, origin あり）/ `develop`（origin あり）
  - `feature/corsi-block`（local+origin）, `feature/capacitor-android`（local のみ）, `feature/nback-game`（local のみ）— **3本とも develop にマージ済み。削除可能な残骸**
- **捻れ（重要）**:
  - main が develop より4コミット先行（マージコミット群）。develop への back-merge がされていない
  - GitHub の**デフォルトブランチは develop**、しかし**デプロイのトリガーは main push**（.github/workflows/deploy.yml）。CLAUDE.md のブランチ戦略とも微妙にずれている（要確認 #5）
- **stash**: なし
- **タグ**: なし。コミットに「v1.3.0相当」とあるが package.json は `0.0.0` のまま。CLAUDE.md のセマンティックバージョニング方針は**運用されていない**
- **PR 運用**: 実質なし（closed 1件のみ、誤方向 develop→main でクローズ）。実際はローカルマージ+直接 push 運用。CLAUDE.md の「main/develop 直接 push 禁止」と乖離
- **feature/nback-game の経緯**: Capacitor 設定コミット（5e764f1）が nback ブランチに混入 → Revert（3ff91d4）→ 独立した feature/capacitor-android（95a4534）に切り直し。歴史としては解決済み

### ドキュメントと実態の乖離

| ドキュメント | 状態 |
|------|------|
| README.md | **存在しない**（公開リポなのに） |
| CLAUDE.md | 神経衰弱1本時代のまま古い: ① `src/games/`（フック層）未記載 ② ルーティング表に nback / corsi なし ③ GitHub Pages デプロイの記載なし ④ Capacitor / android/ の記載なし ⑤ ブランチ運用・バージョニングが実態と乖離 |
| TODO 類 | 存在しない |

### 品質ゲートの実測結果（2026-07-13 実行）

| チェック | 結果 | 詳細 |
|------|------|------|
| `npm run build` | ✅ 成功 | ただし JS 単一バンドル **737KB**（gzip 227KB）で警告。recharts が主因。ルール上の予算（アプリページ < 300KB gzip）は満たすが code-split 余地あり |
| `npm run lint` | ❌ **失敗** | `src/games/nback/useNbackGame.ts` で 2 errors: `runTrial` の宣言前アクセス（setTimeout コールバック内参照なので実行時は動作するが lint は落ちる）+ 2 warnings |
| E2E (`npx playwright test`) | ✅ 9/9 パス | ただしコルシのみ。memory / nback はテストなし |
| ユニットテスト | — | **0本**。テストランナー自体が未導入（vitest なし） |

### デプロイ / 公開

- GitHub Pages（Actions workflow 経由、main push でトリガー）。最終デプロイ 2026-05-09、success
- SPA ルーティング対応済み（404.html リダイレクト方式、`base: /BrainTrain/`）
- モバイルビルドは `--mode mobile` で `base: /` に切り替える構成が既にある

---

## 2. 「氾濫」の分類

### A. 消してよいもの（承認後に実行）

1. ルートの PNG 15枚（デバッグ・確認用スクリーンショット）
2. `test-results/`（空）、`.playwright-mcp/` のログ堆積
3. マージ済み feature ブランチ3本（local の3本 + origin の feature/corsi-block）
4. Vite テンプレ残骸: `src/App.css`, `src/assets/hero.png`, `src/assets/react.svg`, `src/assets/vite.svg`（参照ゼロ確認済み）

### B. 統合・整合させるべきもの

1. **git の捻れ**: main → develop の back-merge、デフォルトブランチとデプロイトリガーの整合（要確認 #5 の判断待ち）
2. **CLAUDE.md** を実態（3ゲーム・フック層・Pages デプロイ・Capacitor）に同期
3. **Docker 一式**の扱い: Pages 運用確立後も残すか（要確認 #4）
4. バージョン管理: タグ運用 or package.json version のどちらかに寄せる（現状どちらも空回り）

### C. 生きているが整理が必要なもの

1. **Capacitor / android/**: モバイル化方針の中核資産。ただしビルド完走の確証がなく、ここが再開ポイント
2. **lint エラー**（useNbackGame.ts）: 実行時は動くが CI 品質ゲートを塞いでいる
3. **E2E のカバレッジ偏り**: corsi のみ。memory / nback は無防備
4. **バンドル 737KB**: recharts の dynamic import 化で改善可能

---

## 3. 次に着手すべきことの優先順位（たたき台）

方向性は「モバイル化 / マネタイズ」。

### 🥇 第1位（推奨）: Capacitor Android ビルドの完走 → 実機確認

- **根拠**: モバイル化という目的に最短で直結し、資産（android/、build:mobile、cap:sync スクリプト）が既に 8 割揃っている。成果物が logs しかない＝最後の 2 割（APK 生成・実機動作）で止まっている可能性が高く、ここが 2 ヶ月休止の詰まりポイントだったと推測される。マネタイズ（AdMob 等）も Capacitor プラグイン経由が本線なので、この完走がマネタイズの前提条件でもある
- **着手前の最小整備**（半日以内、セットで）: lint エラー2件の修正・マージ済みブランチ削除・ルート PNG 掃除。Capacitor 作業中の事故と混乱を防ぐ最低限だけ先行

### 🥈 第2位: リポ衛生の本整備（ドキュメント同期・git 捻れ解消）

- **根拠**: CLAUDE.md の乖離と README 不在は、セッションを跨ぐたびに「現状把握コスト」を再発させる。本レポートを素材に CLAUDE.md 更新+README 新規作成+main/develop 整合をやれば、以降のセッションが軽くなる。ただし単体では製品価値を生まないので第1位より後
- 内容: 分類 A の削除実行、B の統合、CLAUDE.md/README 更新

### 🥉 第3位: マネタイズの最小実験

- **根拠**: 第1位が完了して初めて手段が確定する（Capacitor なら AdMob プラグイン、Web 継続なら AdSense/PWA）。事前にマネタイズの想定（広告 / 買い切り / 寄付）を決める必要あり（要確認 #3）。プライバシーポリシー・ストア要件の調査もここに含む

---

## 4. 要確認リスト（推測で埋めていない事項）

1. **Android ビルドの到達点**: `npx cap sync` → Android Studio でのビルド・実機/エミュレータ起動はどこまで成功した？ 成果物が logs のみなのは「未完走」か「成果物を消しただけ」か
2. **ルートの corsi-*.png / home.png（5月9日）**: コルシ開発時のデバッグ用スクリーンショットと見えるが、残す意図があるか
3. **マネタイズの具体的想定**: 広告（AdMob/AdSense）・買い切りアプリ・寄付のどれを想定しているか。Google Play 配信を狙うか、PWA で済ませるか
4. **Docker 一式（Dockerfile/nginx.conf/docker-compose.yml）**: GitHub Pages 運用確立後も使う場面があるか
5. **デフォルトブランチが develop である意図**: 意図的（開発中心）なら deploy.yml との捻れは許容範囲。意図してないなら main に戻すか、CLAUDE.md のブランチ戦略ごと見直すか
6. **「v1.3.0相当」の管理方法**: タグを打つ運用にするか、package.json の version を使うか

---

## 5. セッション内での回答と方向転換（2026-07-13 追記）

### オーナーの回答（確定事項）

- **Android ビルドの到達点**: 記憶なし（＝未完走扱いでよい）
- **ルート PNG**: 意図なし → 削除対象で確定
- **マネタイズ**: まず広告
- **Docker**: 判断保留
- **デフォルトブランチ develop**: 意図なし（全部任せていた）

### 方向転換（オーナーの構想）

- GitHub Pages は「試しに公開してみただけ」だったが、想定以上に価値を感じた → **本格的にプロダクト化したい**
- **iOS 市場を最初に狙う**。日本語＋英語の2言語
- デザインを整えてストア公開できる品質にしたい。「0から作り直すべきか？」という問いあり
- 市場仮説: ドーパミン中毒・ブレインフォグ等の世論トレンド × 継続スコア可視化 × 通信制約なしの脳トレ → 脳トレブームが来ると本気で見ている
- アーキテクチャ制約: **運用コストを極小化、サーバー費0が理想**。今回の実験で「ローカル完結いけそう」の感触を得ている

### CC の提案（未決定・たたき台）

- 「作り直し」は半分 YES: ゲームロジックのフック層（約670行）と仕様調整の知見は移植、UI/Capacitor Android/Docker は捨てても惜しくない
- **隠れた制約**: 開発機が Windows のため、iOS ビルドには工夫が必要。推奨は **Expo (React Native) + EAS Build**（Mac 不要でクラウドビルド→TestFlight まで確立したワークフロー。無料枠あり）。次点で Capacitor iOS ＋ GitHub Actions macOS ランナー（公開リポなら無料）
- サーバー費0は成立: 端末内 DB（SQLite）＋クライアント直広告＋クライアント i18n。固定費は Apple Developer **$99/年** のみ
- ローカル完結は「データが端末から出ない」というプライバシー訴求として市場仮説と噛み合う（コスト削減以上の意味がある）
- 緊張関係の指摘: ドーパミン中毒対策の思想と広告掲載は矛盾を突かれやすい → 「買い切りで広告除去」ハイブリッドを最初から設計に入れる案。医学的効能の表現はストア審査上避ける
- 進め方案: ①本リポは「プロトタイプ v1」として掃除して凍結（Pages 版はデモとして存置）②新リポで Expo 開始、**「コルシ1本＋日英切替＋TestFlight 配信」を最初のマイルストーン**に ③広告・ストア公開はその後

### 次セッションへの持ち越し（要確認）

→ **同セッション内で全て回答済み・方針確定**。決定内容は [direction-2026-07.md](direction-2026-07.md) を参照（Mac 使用・iPhone あり・$99 許容・スタックは Expo + TypeScript で確定）。以降のセッションは direction 側を起点にすること。
